{
  "name": "agamer-monorepo",
  "private": true,
  "scripts": {
    "start": "cd packages/agamer-gui && npm run start",
    "postinstall": "npm run build-all",
    "build-all": "cd packages/fadb && npm install && cd ../flog && npm install && cd ../agamer && npm install && cd ../agamer-gui && npm install"
  }
}
